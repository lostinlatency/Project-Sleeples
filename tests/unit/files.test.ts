import {describe,it,expect} from 'vitest';import {createInitialState} from '@/lib/director/initial-state';import {canOpen} from '@/lib/filesystem/permissions';import {reduceNarrative} from '@/lib/director/reducer';
describe('protected filesystem',()=>{it('keeps later discoveries locked',()=>{const s=createInitialState('a');expect(canOpen(s,'moving_note')).toBe(true);expect(canOpen(s,'chat_log')).toBe(false);expect(canOpen(s,'warning_note')).toBe(false)});it('unlocks discoveries in sequence',()=>{let s=createInitialState('a');s=reduceNarrative(s,{type:'FILE_OPENED',fileId:'moving_note'}).nextState;expect(canOpen(s,'chat_log')).toBe(true);s=reduceNarrative(s,{type:'FILE_OPENED',fileId:'chat_log'}).nextState;expect(canOpen(s,'warning_note')).toBe(true)});it('only exposes the webcam still after decline',()=>{const s=createInitialState('a');expect(canOpen(s,'webcam_still')).toBe(false);expect(canOpen({...s,routeFlags:{...s.routeFlags,webcamDeclines:1}},'webcam_still')).toBe(true)})});
