'use client';
type SystemSound='startup'|'error'|'message'|'sign-in'|'invite'|'transfer'|'shutdown';let context:AudioContext|null=null;let masterVolume=.65;
const patterns:Record<SystemSound,Array<[number,number,number]>>={startup:[[392,0,.16],[523,.14,.18],[659,.29,.25]],error:[[180,0,.12],[140,.13,.2]],message:[[880,0,.09],[1175,.1,.12]],'sign-in':[[523,0,.1],[784,.1,.16]],invite:[[660,0,.1],[880,.12,.1],[990,.24,.16]],transfer:[[440,0,.08],[554,.09,.08],[659,.18,.12]],shutdown:[[659,0,.15],[523,.16,.18],[392,.34,.28]]};
export function setXpVolume(volume:number){masterVolume=Math.max(0,Math.min(1,volume));}
export function playXpSound(kind:SystemSound){if(masterVolume<=0)return;try{context??=new AudioContext();const ctx=context;if(ctx.state==='suspended')void ctx.resume();for(const [frequency,delay,duration] of patterns[kind]){const oscillator=ctx.createOscillator(),gain=ctx.createGain();oscillator.type=kind==='error'?'square':'sine';oscillator.frequency.value=frequency;gain.gain.setValueAtTime(.025*masterVolume,ctx.currentTime+delay);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+delay+duration);oscillator.connect(gain).connect(ctx.destination);oscillator.start(ctx.currentTime+delay);oscillator.stop(ctx.currentTime+delay+duration);}}catch{}}
