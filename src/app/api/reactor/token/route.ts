import {z} from 'zod';import {openState} from '@/lib/narrative/session-envelope';import {parseJson,apiError,RequestError} from '@/lib/validation/request';
export const runtime='nodejs';const Body=z.object({sessionEnvelope:z.string().min(20)});const TokenResponse=z.object({jwt:z.string().min(1),expires_at:z.number().positive()});const minted=new Map<string,number>();
export async function POST(request:Request){try{const {sessionEnvelope}=Body.parse(await parseJson(request,48_000));const state=await openState(sessionEnvelope);if(!['webcam_preparing','webcam_invite'].includes(state.phase))throw new RequestError('WEBCAM_NOT_READY',403);if(process.env.SLEEPLESS_LTX_MODE!=='live')return Response.json({token:`mock-${state.sessionId}`,expiresAt:Date.now()+900_000,mode:'mock'});if(!process.env.REACTOR_API_KEY)throw new RequestError('REACTOR_NOT_CONFIGURED',503);if((minted.get(state.sessionId)||0)>=1)throw new RequestError('TOKEN_ALREADY_MINTED',429);const response=await fetch('https://api.reactor.inc/tokens',{method:'POST',headers:{'Reactor-API-Key':process.env.REACTOR_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({expires_after:900,authorization_details:[{type:'session',resources:{models:{match:['reactor/ltx2']}},constraints:{max_sessions:1}}]})});if(!response.ok)throw new RequestError('REACTOR_TOKEN_FAILED',502);const data=TokenResponse.parse(await response.json());minted.set(state.sessionId,1);return Response.json({token:data.jwt,expiresAt:data.expires_at*1000,mode:'live'});}catch(error){return apiError(error);}}
