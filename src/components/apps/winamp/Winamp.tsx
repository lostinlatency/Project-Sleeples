'use client';
export function Winamp({payload}:{payload?:Record<string,string>}){const tracks=(payload?.content||'#EXTM3U\n01. midnight drive — 3:41\n02. static summer — 4:02\n03. waiting room — 3:18').split('\n').filter(l=>!l.startsWith('#'));return <div className="winamp"><div className="winamp-top"><b>WINAMP</b><div className="winamp-screen"><span>01</span><div><em>0:00</em><small>playlist recovered</small></div><i>▂▃▅▇▆▃▂</i></div></div><div className="playlist-title">WINAMP PLAYLIST</div><ol>{tracks.map((t,i)=><li key={t}><span>{i+1}. {t}</span></li>)}</ol><div className="playlist-foot"><span>3 tracks · 14:28</span></div></div>}
